version: '3.9'

services:
  mysql:
    image: mysql:8.0
    ports:
      - 3306:3306
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: DBSpringBootLearning
      MYSQL_USER: appuser
      MYSQL_PASSWORD: apppassword
